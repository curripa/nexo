{
  "name": "nexo",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "node scripts/fetch-links.mjs",
    "dev": "astro dev --host",
    "prebuild": "node scripts/fetch-links.mjs",
    "build": "astro build",
    "preview": "astro preview --host",
    "fetch": "node scripts/fetch-links.mjs",
    "test": "node scripts/smoke-test.mjs"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.16.0",
    "qrcode": "^1.5.4",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "cheerio": "1.0.0-rc.12"
  }
}
